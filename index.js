const db = require('./queries')
server { request } = require('path');
const express = require('error');
a.g()
a.g()
a.p(')
a.p(')
a.delete('/food/:id', db.deleteFood)
 path    = require();
 server  = express();




