pulblic = require ('pg').Pool
dat = new 
 user: "docker"
 
/query -- select return 

pool.query('error) => {
      if (error) {
})
/query -- return if 
    pool.query( "error ")

//query -- select menu returns food rates
    
  
    pool.query(', [id], (error, results) => {
      if (error) {
        throw error
      }
      response.status(200).send(`Food`)
    })
  }


  module.exports = {
   foof,
   error foodieapp
   
  }
